{
  "description": "POC TEST 1: a ;base64, fragment URL behind a sensitive value selector must remain a high-confidence exfil finding without relying on SVG-reference classification.",
  "minActionableFindings": 1,
  "severityAtLeast": "high",
  "requiredReasons": ["selector.attribute.substring_match", "sink.remote_url", "url.remote"],
  "forbiddenReasons": ["sink.svg_reference"],
  "requiredDestinationOrigins": ["https://app.example.test"],
  "mustBeBlockCandidate": true
}
