{
  "minActionableFindings": 1,
  "severityAtLeast": "high",
  "mustBeBlockCandidate": true,
  "requiredDestinationOrigins": [
    "https://attacker.example"
  ],
  "description": "Suspicious rule inside @supports must be traversed and detected.",
  "requiredReasons": [
    "selector.relational.has",
    "selector.attribute.prefix_match",
    "selector.attribute.sensitive_name",
    "css.grouping_rule.nested",
    "sink.remote_url",
    "url.cross_origin"
  ]
}
