{
  "description": "Oversized CSS source scanning also inspects nested rule bodies instead of treating a large nested stylesheet as safe.",
  "minActionableFindings": 1,
  "severityAtLeast": "high",
  "requiredReasons": ["selector.attribute.substring_match", "selector.attribute.sensitive_name", "sink.remote_url", "css.grouping_rule.nested"],
  "forbiddenReasons": ["analysis.skipped.too_large"],
  "requiredDestinationOrigins": ["https://attacker.example"],
  "mustBeBlockCandidate": true
}
