{
  "description": "POC TEST 2.1: fallback CSS variable URL indirection behind a sensitive value selector must be a high-confidence exfil finding.",
  "minActionableFindings": 1,
  "severityAtLeast": "high",
  "requiredReasons": ["selector.attribute.substring_match", "sink.remote_url", "css.custom_property.unresolved", "url.remote"],
  "requiredDestinationOrigins": ["https://app.example.test"],
  "mustBeBlockCandidate": true
}
