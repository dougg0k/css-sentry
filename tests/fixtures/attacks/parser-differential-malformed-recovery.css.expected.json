{
  "description": "Malformed-but-recoverable parser fixture with missing semicolons; conservative parsing should still detect the remote sink and sensitive selector.",
  "minActionableFindings": 1,
  "severityAtLeast": "high",
  "requiredReasons": [
    "selector.attribute.prefix_match",
    "selector.attribute.sensitive_name",
    "sink.remote_url",
    "url.cross_origin",
    "css.grouping_rule.nested"
  ],
  "requiredDestinationOrigins": ["https://attacker.example"],
  "mustBeBlockCandidate": true
}
